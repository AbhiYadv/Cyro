{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-stream-event-listener",
  "description": "Allow the main Cyro window to subscribe to Rust-owned local runtime stream events and remove listeners after completion.",
  "windows": ["main"],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten"
  ]
}
